<% if @result %> 
  location.href = '/galleries'
<% else %>
  $('.error-msg').html("<%= j(render 'shared/errors', resource: @gallery) %>");
  $('#submit-button').show();
  $('#spinner-button').hide();
<% end %>